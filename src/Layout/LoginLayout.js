import React from 'react';

const LoginLayout = (props) => {

    return (
        <React.Fragment>
            {props.children}
        </React.Fragment>
        
    );
}

export default LoginLayout;